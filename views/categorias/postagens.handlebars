{{#each postagens}}
<div class="card">
    <div class="card-body">
        <h3>{{titulo}}</h3>
        <p>{{descricao}}</p>
        <a href=""><button class="btn btn-sucess">leia sua bunda </button></a>
        <hr>
        <small>posts: {{Categoria.nome}}</small>
        <small>data de pulbicçao: {{data}}</small>
    </div>
</div>
{{else}}
<h5> nenhuma postagens</h5>
{{/each}}